
<%= form_for :user, url: destroy_user_session_path, method: :delete, html: { class: "navbar-form navbar-right" } do |f| %>
  <div class="form-group">
    <button type="submit" class="btn btn-success">Logout</button>
  </div>
<% end %>
